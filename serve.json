{
  "_comment": "DEPRECATED for app runtime. Canonical frontend is Next.js App Router (npm run dev/build/start). Legacy static rewrites intentionally disabled to avoid serving stale HTML routes.",
  "rewrites": []
}
