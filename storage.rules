rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated admin can upload/read/delete content
    match /content/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
